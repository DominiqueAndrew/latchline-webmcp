{
  "name": "latchline-webmcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173 -d .",
    "build": "node scripts/build-check.mjs",
    "evaluate": "node scripts/evaluate.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run test && npm run build"
  }
}
